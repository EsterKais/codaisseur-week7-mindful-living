<nav class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Mindful Living", root_path, class: "navbar-brand" %>
    </div>

    <div id="navbar">
      <ul class="nav navbar-nav navbar-right">

        <% if !user_signed_in? %>
          <li><%= link_to "Log In", new_user_session_path %></li>
          <li><%= link_to "Sign Up", new_user_registration_path %></li>
        <% else %>
            <li><%= link_to current_user.full_name, profile_path(current_user.profile.id) %></li>
            <li><%= link_to "Wishlist", 'wishlist/index' %></li>
            <li role="separator" class="divider"></li>

            <% if current_user.has_profile? %>
              <li><%= link_to "Your Profile", profile_path(current_user.profile.id) %></li>
            <% end %>

            <li role="separator" class="divider"></li>
            <li><%= link_to "Log out", destroy_user_session_path, method: :delete %></li>

      </ul>


        <% end %>
    </div><!--/.nav-collapse -->
  </div>
</nav>
